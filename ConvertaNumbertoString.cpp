#include <iostream>
#include <math.h>
#include <vector>
#include <map>
#include <sstream>
#include <limits>
using namespace std;

string number_to_string(int num) {
	return to_string(num);
}